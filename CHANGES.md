# دفتر التسليم — تعديلات جيبتك

هذا الملف سجل مقروء للتعديلات اللي تصير على المشروع خارج بيئة ChatGPT (عبر Claude).
الهدف: لما يكمّل ChatGPT على المشروع، يقرأ هذا الملف أول شي ويكون فاهم شو تغيّر، وين، وليش.

## بنية الفروع

- `codex/jeebtak` — الأساس. شغل ChatGPT الأصلي كما هو، بدون أي تعديل من Claude. نقطة مرجعية.
- `jad/claude-edits` — فرع العمل. كل تعديلات Claude تصير هنا، كل واحد commit صغير مستقل.

## كيف تقرأ هذا الملف (لـ ChatGPT)

1. كل مدخلة تحت «السجل» = تعديل واحد، مرتبة من الأحدث للأقدم.
2. راجع الملفات المذكورة و`git log` على فرع `jad/claude-edits` لتفاصيل الأسطر.
3. الاختبارات تنفَّذ بـ `node --test tests/core.test.cjs tests/pwa.test.cjs tests/ui.test.cjs` من جذر المشروع. حالة الأساس: 35 اختبار، كلها ناجحة.
4. لو أكملت من هنا، أضف مدخلاتك بنفس الشكل عشان يبقى السجل متصل.

## السجل

<!-- المدخلات الجديدة تضاف فوق هذا السطر -->

### إضافة «مدفوعة حتى» للفواتير والالتزامات

- **شو:** حقل اختياري جديد `settledThrough` (شهر `YYYY-MM`) على الفواتير والالتزامات. كل استحقاق شهره ≤ هذا الشهر يُعتبر مسدّداً: لا يظهر في «تحتاج انتباهك»، ولا يطلب تأكيداً، **ولا تُسجَّل له أي حركة نقدية**. الشهور بعده تظهر كالمعتاد.
- **الملفات:**
  - `public/core.js` — `occurrences()`: تبدأ حلقة الأشهر من الشهر اللي بعد `settledThrough`، مع فحص `m<=x.settledThrough` كطبقة أمان. `validate()`: تقبل `settledThrough` غير موجود، أو شهر `YYYY-MM` صالح؛ وترفض أي صيغة أخرى.
  - `public/app.js` — `billForm` و `debtForm`: حقل «مدفوعة/مدفوع حتى نهاية شهر» (`type="month"`, `max` = الشهر الحالي) مع تحقّق. `bills()`: شارة «مسدّدة سابقاً» بدل زر «تسجيل الدفع» للشهور المسدّدة. `debts()`: سطر «مسدّد حتى <شهر>» على البطاقة.
  - `public/sw.js` — `CACHE` من `jeebtak-app-v1` إلى `jeebtak-app-v2` ليأخذ العملاء `core.js`/`app.js` الجديدين.
- **السبب:** إضافة فاتورة/التزام بتاريخ بدء قديم كانت تولّد استحقاقاً غير مدفوع لكل شهر منذ ذلك التاريخ، فتمتلئ الإشعارات بعشرات البنود المتأخرة التي دفعها المستخدم فعلياً خارج التطبيق. الحل: المستخدم يحدّد الشهر المسدّد حتى، بدون إنشاء دفعات وهمية تؤثر على الرصيد أو التقارير.
- **مثال:** فاتورة «zain private» بدأت 2022، مدفوعة فعلياً حتى تموز 2026 → تعيين `settledThrough='2026-07'` يخفي كل الأشهر حتى تموز، ويُبقي آب وأيلول ظاهرَين لأنهما غير مدفوعَين.
- **ما لم يتغيّر:** لا هجرة بيانات لازمة (الحقل اختياري وغيابه = السلوك القديم). النسخ الاحتياطية القديمة تُستورد كما هي. لا حركات مالية جديدة، ولا مساس بالرصيد أو صافي الراتب أو تقارير المصروف.
- **الاختبارات:** `node --test tests/` → 40/40 ناجحة (35 سابقة + 5 جديدة: تغطية للفواتير والالتزامات و`validate` واختبار واجهة يضبط الحقل ويتحقق من اختفاء الزر).

### الإعداد الأولي — ربط GitHub

- **شو:** ربط المشروع بمستودع GitHub خاص (`snapjad/jeebtak`) وإنشاء فرع العمل `jad/claude-edits`، وإضافة هذا الملف.
- **الملفات:** `CHANGES.md` (جديد) فقط. لا تعديل على كود التطبيق.
- **ليش:** نقطة مشتركة يقرأ منها الطرفان (Claude و ChatGPT) بدل نقل ملفات يدوي.
- **الاختبارات:** لم تتأثر — 35/35 ناجحة.
