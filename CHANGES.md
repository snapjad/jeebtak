# دفتر التسليم — تعديلات جيبتك

## 2026-09-12 — مزامنة خاصة اختيارية

- أضيفت خدمة Cloudflare لمحفظة واحدة، مع مصادقة بمفتاح خاص وتخزين AES-GCM مشفّر قبل الرفع. لا بيانات أو مفاتيح شخصية داخل المستودع.
- أضيف ربط الأجهزة بالرمز من الإعدادات، حالة مزامنة كل خمس ثوانٍ عند فتح التطبيق، واسترجاع العمل بعد انقطاع الاتصال.
- رقم مراجعة يمنع استبدال بيانات أحدث. التعديلات المتعارضة تحتاج اختيار النسخة بعد مراجعتها، مع تنزيل النسختين ونسخة إنقاذ قبل الاستبدال. بقيت المعاملات مترابطة دون دمج تلقائي خطير.
- PIN والمظهر خاصان بالجهاز. الاستيراد والمسح يحتاجان فصل المزامنة أولاً. لا شبكة قبل الربط؛ البيانات القديمة لم تُعدّل أو تُرفع تلقائياً.
- تحديث service worker إلى v5، وتوسيع اختبارات التشفير والتعارضات والتخزين والمصادقة. تعليمات التشغيل والحدود في SYNC.md.

هذا الملف سجل مقروء للتعديلات اللي تصير على المشروع خارج بيئة ChatGPT (عبر Claude).
الهدف: لما يكمّل ChatGPT على المشروع، يقرأ هذا الملف أول شي ويكون فاهم شو تغيّر، وين، وليش.

## بنية الفروع

- `codex/jeebtak` — الأساس. شغل ChatGPT الأصلي كما هو، بدون أي تعديل من Claude. نقطة مرجعية.
- `jad/claude-edits` — فرع العمل. كل تعديلات Claude تصير هنا، كل واحد commit صغير مستقل.

## كيف تقرأ هذا الملف (لـ ChatGPT)

1. كل مدخلة تحت «السجل» = تعديل واحد، مرتبة من الأحدث للأقدم.
2. راجع الملفات المذكورة و`git log` على فرع `jad/claude-edits` لتفاصيل الأسطر.
3. الاختبارات تنفَّذ بـ `node --test tests/core.test.cjs tests/pwa.test.cjs tests/ui.test.cjs` من جذر المشروع. حالة الأساس: 35 اختبار، كلها ناجحة.
4. لو أكملت من هنا، أضف مدخلاتك بنفس الشكل عشان يبقى السجل متصل.

## السجل

<!-- المدخلات الجديدة تضاف فوق هذا السطر -->

### فصل تفاصيل القرض البنكي عن حساب الالتزام البسيط

- **شو:** دعم اختياري لجدول البنك: نسبة السداد المدخلة، نهاية العقد، مدته، APR، مجموع الفوائد والرسوم والقسط الأخير. أصل القرض مستقل عن الفوائد، والنسبة البنكية مستقلة عن نسبة انخفاض الأصل.
- **الدفع:** للديون البنكية يطلب الرصيد بعد القسط حسب كشف البنك؛ تُسجل الدفعة كاملة كتدفق نقدي، ويخفض رصيد الدين فقط بالفرق المبلّغ. التراجع يعيد نفس الفرق. الجدول يحترم تاريخ ومبلغ القسط الأخير.
- **الملفات:** public/app.js وpublic/core.js وpublic/styles.css؛ ذاكرة التطبيق v4 في public/sw.js. حقول اختيارية، وبيانات النسخ القديمة محفوظة دون هجرة تلقائية.
- **الخصوصية:** لا توجد بيانات قرض شخصية داخل المصدر أو الاختبارات. تصحيح سجل المستخدم يتم محلياً من الواجهة.
- **الاختبارات:** 48 اختباراً، تشمل فصل نسبة البنك وأثر الدفعة على النقد وأصل الدين.


### تشغيل النشر التلقائي على GitHub Pages

- **شو:** الموقع صار حيّ على **https://snapjad.github.io/jeebtak/** ، وكل push على `jad/claude-edits` ينشر تلقائياً بعد نجاح الاختبارات.
- **تغييرات على جهة GitHub (مش في الكود):**
  - المستودع `snapjad/jeebtak` صار **public** (كان private؛ GitHub Pages ما بيشتغل على private إلا بخطة مدفوعة). لا أسرار في الكود.
  - GitHub Pages مُفعّل بنوع build = `workflow` (النشر عبر Actions).
  - بيئة `github-pages`: أُضيف `jad/claude-edits` لسياسة فروع النشر (كان `codex/jeebtak` فقط، وهو سبب فشل أول تشغيلات النشر).
  - متغيّر المستودع `DEPLOY_TO_PAGES = true` (وظيفة `deploy` تُتخطّى إذا لم يكن `true`).
- **الملفات:** `.github/workflows/deploy.yml` فقط (وظيفة `test` على كل push، ووظيفة `deploy` خلف المتغيّر). لا تغيير على كود التطبيق.
- **ملاحظة لـ ChatGPT:** إذا انتقل العمل لفرع آخر، حدّث `branches:` في الـ workflow وأضف الفرع لسياسة فروع بيئة `github-pages`. الرابط القديم على استضافة OpenAI (`.openai/hosting.json`) لم يُلمس ويبقى منفصلاً.

### تحديد جماعي في الإشعارات: «اعتبر المحدّد مسدّداً»

- **شو:** في شاشة «التنبيهات» تحت «تحتاج انتباهك»: مربّع اختيار لكل استحقاق + «تحديد الكل» + زر «اعتبر المحدّد مسدّداً». الزر يعلّم كل البنود المحدّدة كمسدّدة **بدون تسجيل أي حركة مالية**، لكل الأنواع.
  - **فواتير/التزامات:** يرفع `settledThrough` لأحدث شهر محدّد لذلك البند.
  - **اشتراكات:** يقدّم `nextDate` دورةً دورةً حتى يتجاوز أحدث تجديد محدّد (تخطّي بلا دفعة). الاشتراكات المجمّدة/الملغاة تُتجاهل.
- **الملفات:**
  - `public/core.js` — دالة جديدة `settleOccurrences(s, items)` مُصدَّرة في `api`. تأخذ قائمة استحقاقات، تجمّعها لكل بند، وتطبّق أحدث واحد. تتجاهل المعرّفات غير الموجودة.
  - `public/app.js` — `upcomingItems()` بارامتر `pick` يعرض `input.settle-pick` بقيمة `type:id:period`. `notifications()` يعرض شريط التحديد ويمرّر `pick=true`. إجراء `settle-picked` يجمع المحدّد ويؤكّد عبر `confirmAction` ثم `J.settleOccurrences`. مستمع `change` يتعامل مع `#settle-all` (تحديد/إلغاء الكل، DOM فقط).
  - `public/styles.css` — تنسيق `.bulk-settle-bar` و `.settle-pick`.
  - `public/sw.js` — `CACHE` إلى `jeebtak-app-v3`.
- **السبب:** حتى مع «مدفوعة حتى»، تصفية تراكم قديم كبير كانت تتطلب فتح كل بند على حدة. هذا يصفّيها دفعة واحدة، وبنفس المبدأ: لا حركات وهمية، لا مساس بالرصيد أو التقارير.
- **الاختبارات:** `node --test tests/` → 44/44 (35 أصلية + 5 «مدفوعة حتى» + 4 هنا: فواتير/التزامات، اشتراكات، تجاهل غير الصالح، واختبار واجهة كامل للتدفّق).

### إضافة «مدفوعة حتى» للفواتير والالتزامات

- **شو:** حقل اختياري جديد `settledThrough` (شهر `YYYY-MM`) على الفواتير والالتزامات. كل استحقاق شهره ≤ هذا الشهر يُعتبر مسدّداً: لا يظهر في «تحتاج انتباهك»، ولا يطلب تأكيداً، **ولا تُسجَّل له أي حركة نقدية**. الشهور بعده تظهر كالمعتاد.
- **الملفات:**
  - `public/core.js` — `occurrences()`: تبدأ حلقة الأشهر من الشهر اللي بعد `settledThrough`، مع فحص `m<=x.settledThrough` كطبقة أمان. `validate()`: تقبل `settledThrough` غير موجود، أو شهر `YYYY-MM` صالح؛ وترفض أي صيغة أخرى.
  - `public/app.js` — `billForm` و `debtForm`: حقل «مدفوعة/مدفوع حتى نهاية شهر» (`type="month"`, `max` = الشهر الحالي) مع تحقّق. `bills()`: شارة «مسدّدة سابقاً» بدل زر «تسجيل الدفع» للشهور المسدّدة. `debts()`: سطر «مسدّد حتى <شهر>» على البطاقة.
  - `public/sw.js` — `CACHE` من `jeebtak-app-v1` إلى `jeebtak-app-v2` ليأخذ العملاء `core.js`/`app.js` الجديدين.
- **السبب:** إضافة فاتورة/التزام بتاريخ بدء قديم كانت تولّد استحقاقاً غير مدفوع لكل شهر منذ ذلك التاريخ، فتمتلئ الإشعارات بعشرات البنود المتأخرة التي دفعها المستخدم فعلياً خارج التطبيق. الحل: المستخدم يحدّد الشهر المسدّد حتى، بدون إنشاء دفعات وهمية تؤثر على الرصيد أو التقارير.
- **مثال:** فاتورة «zain private» بدأت 2022، مدفوعة فعلياً حتى تموز 2026 → تعيين `settledThrough='2026-07'` يخفي كل الأشهر حتى تموز، ويُبقي آب وأيلول ظاهرَين لأنهما غير مدفوعَين.
- **ما لم يتغيّر:** لا هجرة بيانات لازمة (الحقل اختياري وغيابه = السلوك القديم). النسخ الاحتياطية القديمة تُستورد كما هي. لا حركات مالية جديدة، ولا مساس بالرصيد أو صافي الراتب أو تقارير المصروف.
- **الاختبارات:** `node --test tests/` → 40/40 ناجحة (35 سابقة + 5 جديدة: تغطية للفواتير والالتزامات و`validate` واختبار واجهة يضبط الحقل ويتحقق من اختفاء الزر).

### الإعداد الأولي — ربط GitHub

- **شو:** ربط المشروع بمستودع GitHub خاص (`snapjad/jeebtak`) وإنشاء فرع العمل `jad/claude-edits`، وإضافة هذا الملف.
- **الملفات:** `CHANGES.md` (جديد) فقط. لا تعديل على كود التطبيق.
- **ليش:** نقطة مشتركة يقرأ منها الطرفان (Claude و ChatGPT) بدل نقل ملفات يدوي.
- **الاختبارات:** لم تتأثر — 35/35 ناجحة.
