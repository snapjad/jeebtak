# المزامنة الخاصة

الواجهة تبقى على GitHub Pages. خدمة Cloudflare Workers مع SQLite Durable Object تحفظ محفظة واحدة مشفّرة. لا بريد ولا شاشة حساب داخل جيبتك؛ يحتاج كل جهاز رمز الربط مرة واحدة. حساب Cloudflare مطلوب لمالك الخدمة فقط.

## الاستخدام

1. صدّر JSON من الجهاز الذي يحتوي أحدث بياناتك.
2. على هذا الجهاز: الإعدادات ← ربط بالمحفظة الخاصة ← لصق رمز الربط. أول جهاز يرفع النسخة الأولى.
3. على الجهاز الآخر، افتح نفس التطبيق ثم اربطه بنفس الرمز. إذا اختلفت النسختان، راجعهما واختر السحابية لتنزيل نسخة الجهاز الأول، أو المحلية لاعتماد نسخة هذا الجهاز على بقية الأجهزة. لا يتم دمج الدفعات تلقائياً.
4. المزامنة تتم كل خمس ثوانٍ أثناء فتح التطبيق وعدم تحرير نموذج. التطبيق المغلق على iOS لا يبقى متصلاً؛ تعود المزامنة عند فتحه وإزالة قفل PIN.

النسخ غير المختارة لا تُدمج. نزّل نسختي التعارض للمراجعة قبل الاختيار. نسخة الإنقاذ المحلية تحتوي النسختين في ملف واحد، وتُستبدل عند قرار التعارض التالي. الاحتفاظ بنسخ JSON مستقلة يبقى مهماً. فصل الجهاز يوقف مزامنته ويبقي بياناته ونسخة السحابة. الاستيراد والمسح يحتاجان فصل الجهاز أولاً.

## حماية البيانات

- مفتاح عشوائي 256 بت. اشتقاق HKDF-SHA256 بمعلومات منفصلة للتشفير والمصادقة. AES-256-GCM مع IV عشوائي جديد لكل رفع.
- مفتاح التشفير ورمز الربط لا يرسلان إلى الخدمة، ولا يدخلان نسخة JSON العادية، ولا عنوان URL أو مستودع Git. الرمز يخزن محلياً على الأجهزة المربوطة؛ من يملكه يستطيع القراءة والكتابة. قفل PIN خاص بالواجهة ولا يشفر التخزين المحلي.
- النقل HTTPS. رمز مصادقة مشتق في Authorization، والخدمة تحفظ فقط بصمته كـ Cloudflare Secret. المصادقة تسبق الوصول إلى التخزين؛ لا إنشاء محافظ عشوائي للعامة. CORS مقيد بأصل GitHub Pages.
- تحقق تفاؤلي برقم النسخة (CAS) يمنع استبدال تعديل حديث بواسطة جهاز قديم. تغيّر الطرفين يوقف المزامنة ويحتاج قراراً صريحاً. هذا مقصود لحماية ترابط الأقساط والمعاملات.
- يبقى PIN والمظهر على كل جهاز. المالية وبقية إعدادات المحفظة تتزامن. تُراجع النسخة المفكوكة بنفس قواعد استيراد التطبيق قبل تطبيقها. سجل استرداد يحمي من فشل الحفظ بين البيانات ومعلومات المزامنة.
- حد النسخة المالية قبل التشفير 700,000 بايت. الخدمة تحتفظ بآخر خمس نسخ مشفّرة للتعافي الإداري. لا سجل وصول مفعّل في إعدادات Worker. Cloudflare يظل قادراً على معرفة بيانات الاتصال وحجم المحتوى المشفّر.
- فقدان الرمز وجميع الأجهزة المربوطة يمنع فك النسخة السحابية. تغيير المفتاح وإلغاء أجهزة بعينها ليسا واجهة متاحة بهذا الإصدار؛ يمكن إبطال مصادقة جميع الأجهزة من سر الخدمة ثم تجهيز محفظة جديدة من JSON.

## تشغيل الخدمة وصيانتها

الخدمة في `sync-worker/worker.mjs` وإعدادها `sync-worker/wrangler.jsonc`. النشر يحتاج Wrangler وحساب مالك الخدمة. لا تحفظ رمز الربط في `public/` أو متغيرات JavaScript العامة. لا تستخدم تخزين KV متأخر الاتساق للمراجعات.

اختبارات Node 24: `node --test tests/core.test.cjs tests/pwa.test.cjs tests/ui.test.cjs tests/sync.test.cjs tests/sync-worker.test.cjs`.

الواجهة تبقى دون build step. `sync.js` أصل محلي مخزن للعمل بلا اتصال، وطلبات خدمة المزامنة الخارجية لا تدخل service worker cache. لا تبدأ أي طلبات مزامنة قبل الربط الصريح.
